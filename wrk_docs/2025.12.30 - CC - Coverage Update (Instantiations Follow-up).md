# 2025.12.30 — Code Coverage Update (Instantiations Follow‑up)

## Canonical run (unchanged)

This update continues to use the canonical command (always with a timeout) that has been used for recent work:

```bash
timeout 1800 cargo llvm-cov --no-default-features --features cli,llvm,native --summary-only --json --output-path target/llvm-cov-summary.json
```

## Current totals (canonical)

- **Lines:** 62939 / 63044 (**99.8334%**)
- **Regions:** 111329 / 111701 (**99.6670%**) — **372** uncovered regions
- **Functions:** 3437 / 3437 (**100%**)
- **Instantiations:** 6044 / 6044 (**100%**)

### Key files (canonical)

- `src/main.rs`: lines **946/948**, regions **1824/1832** (uncovered regions: **8**)
- `src/error.rs`: lines **902/903**, regions **1212/1213** (uncovered regions: **1**)
- `src/compiler.rs`: lines **1519/1521**, regions **2679/2681** (uncovered regions: **2**)
- Hotspots (unchanged):
  - `src/interpreter.rs`: uncovered regions **177**
  - `src/llvm/codegen.rs`: uncovered regions **180**

## Changes in this iteration

### Dependency‑instance tests

- Updated `tests/error_suggestion_instantiation_coverage_only.rs`:
  - Removed failure‑only formatted `assert!` messages (to avoid introducing coverage-only regions on the failure path).
  - Added an additional test covering the `UndefinedVariable { name: "String" }` suggestion path in the dependency instance.
- Added `tests/compiler_for_loop_instantiation_coverage_only.rs`:
  - Exercises compilation of a `fer ... in ... { ... }` loop in a dependency‑instance test.

## Result

- The canonical totals and the per‑file uncovered counts above are **unchanged** after these additions.
- Hard constraints remain satisfied:
  - `functions.percent == 100`
  - `instantiations.percent == 100`
  - Hotspot ordering preserved: **interpreter (177) < codegen (180)**.

## Next steps (recommended)

1. **Pinpoint the remaining misses per instantiation** (especially for `src/main.rs`, `src/error.rs`, `src/compiler.rs`) using an instantiation-aware report and/or instantiation HTML output, then target *those* specific instantiations/branches.
2. Once the small-file misses are understood, **cool the remaining hotspot** (primarily `src/llvm/codegen.rs` and `src/interpreter.rs`) while preserving the ordering constraint (**interpreter missed regions must stay below codegen**).

