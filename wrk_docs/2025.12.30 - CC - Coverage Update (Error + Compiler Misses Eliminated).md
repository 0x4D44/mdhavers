# Coverage Update — Error + Compiler Misses Eliminated

Date: 2025.12.30  
Canonical scoreboard source: `target/llvm-cov-summary.json`

## Canonical command (always run with a timeout)

```bash
timeout 1800 cargo llvm-cov --no-default-features --features cli,llvm,native --summary-only --json --output-path target/llvm-cov-summary.json
```

## Result (canonical totals)

From `target/llvm-cov-summary.json`:

- Lines: **62966 / 63068** (**99.8383%**) — **102** uncovered
- Regions: **111362 / 111731** (**99.6697%**) — **369** uncovered
- Functions: **3439 / 3439** (**100%**)
- Instantiations: **6046 / 6046** (**100%**)

### Floors / invariants preserved

- `functions.percent == 100` ✅
- `instantiations.percent == 100` ✅
- Hotspot ordering preserved ✅
  - `src/interpreter.rs` missed regions: **177**
  - `src/llvm/codegen.rs` missed regions: **180**
  - Ordering: **177 < 180**

## What changed

### Small-file misses eliminated

These were the remaining “small per-file” misses targeted in Stage 1A/1B of the plan:

- `src/error.rs`: **now 100%** (lines + regions)
- `src/compiler.rs`: **now 100%** (lines + regions)

### Why earlier integration-only attempts didn’t move the needle

The remaining misses were in the best-covered **unit-test instantiations** (crate hash `Cs2Pex…` in the `llvm-cov` function listing). Adding integration tests that exercised the same logic in a different instantiation did not affect the per-file summary’s chosen “best” instance.

### Targeted changes made

- `src/error.rs`: extended the existing unit tests to exercise the `UnexpectedToken` “default/no-suggestion” path.
- `src/compiler.rs`:
  - added a unit test that compiles a call where the callee is a `Get` (e.g. `foo.bar(1)`), exercising the “constructor heuristic skip” path in `compile_expr`.
  - added a unit test that compiles a `Class` AST containing a non-function entry in `methods`, exercising the non-matching `if let Stmt::Function { .. }` path in `compile_stmt`.
- `tests/compiler_instantiation_edge_paths_coverage_only.rs`: silenced `unused_must_use` warnings by binding `black_box(...)` results to `_`.
- `tests/error_suggestion_instantiation_coverage_only.rs`: same `unused_must_use` fix for the new `black_box(...)` call.

## Remaining hotspots / next steps

### Stage 1C — `src/main.rs` (still small)

Current canonical miss remains:

- `src/main.rs`: lines **946/948**, regions **1824/1832** (uncovered regions **8**)

Next action: locate the exact missed branch(es) in the “best” instantiation and add focused CLI/unit coverage to hit them (prefer deterministic CLI/integration tests where applicable).

### Stage 2 — Hotspot work

After `src/main.rs` is cleaned up, proceed to the large hotspots while maintaining:

- `src/interpreter.rs` missed regions **<** `src/llvm/codegen.rs`

