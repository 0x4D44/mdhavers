# Code Coverage Review (LLVM-Cov)

Date: 2025.12.28  
Repo: `mdhavers`  
Scope: Rust coverage under `cargo llvm-cov` (LLVM source-based coverage)

## Canonical measurement command (do not change)

```
cargo llvm-cov --no-default-features --features cli,llvm,native --summary-only --json --output-path target/llvm-cov-summary.json
```

## Current totals (from `target/llvm-cov-summary.json`)

- **Lines:** 60315 / 60584 = **99.5560%**
- **Functions:** 3236 / 3236 = **100%**
- **Regions:** 107516 / 108395 = **99.1891%**
- **Instantiations:** 5773 / 5773 = **100%**

## Summary

- The repo is already in a *very strong* coverage posture: **functions and instantiations are both 100%**, and overall lines are **>99.5%**.
- The remaining tail is primarily **region coverage** (~0.81% uncovered). In practice, this tail is dominated by:
  - defensive / error-only branches (`?`, `compile_ok_or(...)`, and “should never happen” code)
  - hard-to-trigger IO/concurrency failures
  - large compiler backends where each small missing branch counts against a very large region denominator

## Biggest remaining region tails (workspace `src/` only)

The table below is derived from `target/llvm-cov-export.json` (full export) and shows the biggest *absolute* region gaps.

| File | Regions | Regions % | Missed regions | Lines % | Missed lines |
|---|---:|---:|---:|---:|---:|
| `src/interpreter.rs` | 26027 / 26328 | 98.857% | 301 | 98.957% | 168 |
| `src/llvm/codegen.rs` | 54310 / 54645 | 99.387% | 335 | 99.770% | 65 |
| `src/parser.rs` | 3183 / 3206 | 99.283% | 23 | 99.783% | 4 |
| `src/wasm_compiler.rs` | 2443 / 2473 | 98.787% | 30 | 99.426% | 9 |
| `src/audio.rs` | 3661 / 3721 | 98.388% | 60 | 100.000% | 0 |
| `src/llvm/compiler.rs` | 816 / 854 | 95.550% | 38 | 97.884% | 12 |

## Notes on what the remaining misses likely represent

This repo has a lot of coverage-only work already; the remaining region misses tend to be in categories that are expensive (or undesirable) to force:

1. **Error propagation branches**
   - Many regions come from `Result`/`Option` handling (`?`, `ok_or`, `compile_ok_or`).
   - In many cases, the “Err” side exists for correctness but is not something we *want* to trigger broadly (e.g., builder failures, IO failures).

2. **Backend-specific branches**
   - `src/llvm/codegen.rs` is very large; even a small percentage gap is hundreds of regions.
   - Some compilation branches are sensitive to inferred types / “fast path” eligibility.

3. **Runtime edge cases**
   - `src/interpreter.rs` still has a moderate region tail; many branches here are likely error-only or integration edges.

## How to drill down (repeatable workflow)

1. Generate a full export (used for the file table above):
   ```
   cargo llvm-cov report --json --output-path target/llvm-cov-export.json
   ```
2. Generate an HTML report with missing lines for interactive triage:
   ```
   cargo llvm-cov --no-default-features --features cli,llvm,native --html --show-missing-lines
   ```
3. Prefer addressing misses that correspond to *real, stable* behavior:
   - deterministic unit/integration tests
   - coverage-only tests under `#![cfg(coverage)]` for pure “hit the branch” exercises
   - small refactors removing unreachable branches (only when invariants truly guarantee unreachable)
