# Coverage Update (LLVM-Cov)

Date: 2025.12.30  
Scoreboard source of truth: `target/llvm-cov-summary.json`

## Canonical command

```bash
timeout 1800 cargo llvm-cov --no-default-features --features cli,llvm,native --summary-only --json --output-path target/llvm-cov-summary.json
```

## Current totals (from `target/llvm-cov-summary.json`)

- Lines: **62987 / 63087** (**99.8415%**) — 100 missed
- Regions: **111424 / 111783** (**99.6788%**) — 359 missed
- Functions: **3442 / 3442** (**100%**) ✅
- Instantiations: **6049 / 6049** (**100%**) ✅

## Hotspots (src-only, regions missed)

| File | Lines missed | Regions missed |
|---|---:|---:|
| `src/llvm/codegen.rs` | 6 | 178 |
| `src/interpreter.rs` | 94 | 177 |
| `src/wasm_compiler.rs` | 0 | 2 |
| `src/value.rs` | 0 | 1 |
| `src/parser.rs` | 0 | 1 |

**Invariant preserved:** `src/interpreter.rs` missed regions **<** `src/llvm/codegen.rs` missed regions (**177 < 178**).

## What changed in this update

`tests/llvm_codegen_gap_coverage.rs`:

1. Added a compile-error case that forces the **list-shadow shove fast path** to execute the `?` error-propagation branch:
   - Inside a function, create a list-shadow local (`ken xs = [1]`), then call `shove(xs, no_such_var)`.
2. Added a compile-error case that forces the **standard shove path** to execute the `?` error-propagation branch for `compile_expr(&args[0])` when the list is inferred as `List`, but compilation fails:
   - `shove([1, no_such_var], 2)`

## Result

- `src/llvm/codegen.rs` regions not covered: **180 → 178** (net **-2**)
- Floors held: **functions == 100%**, **instantiations == 100%**
- Ordering held: **interpreter (177) < codegen (178)**

