# 2026.01.05 — Code Coverage Review (mdhavers)

## Scope

This review focuses on the Rust crate coverage produced by the current LLVM coverage workflow (via `cargo llvm-cov`) and the repo’s coverage guardrails.

## How to reproduce

- Guardrails (summary + invariants):
  - `timeout 2400 bash scripts/coverage_guardrails.sh`
- Full coverage JSON (for per-line hotspot analysis):
  - `timeout 2400 cargo llvm-cov --no-default-features --features cli,llvm,native --no-run --json --output-path target/llvm-cov-full.json`
- Summary JSON:
  - `target/llvm-cov-summary.json`

## Current totals (guardrails run)

From `target/llvm-cov-summary.json`:

- **Functions:** 3537 / 3537 (100%)
- **Instantiations:** 6179 / 6179 (100%)
- **Lines:** 69263 / 69389 (99.8184150226%)
- **Regions:** 124692 / 124991 (99.7607827764%), **299 uncovered**

These exceed the project’s “>= 98%” target while keeping the strict floors at 100% for functions + instantiations.

## Hotspots (regions not covered)

Top files by `regions.notcovered`:

1. `src/llvm/codegen.rs`: **148**
2. `src/interpreter.rs`: **148**
3. `src/wasm_compiler.rs`: **2**
4. `src/parser.rs`: **1**

Everything else in `src/` is at 0 uncovered regions in the current run.

### `src/llvm/codegen.rs` hotspot details

This file is still the dominant uncovered-region hotspot despite being >99% covered:

- **Lines:** 34685 / 34729 (99.8733047309%)
- **Regions:** 67556 / 67704 (99.7814013943%), **148 uncovered**

Using `target/llvm-cov-full.json`, the highest-frequency uncovered line starts in this file are:

- The previous top hotspot (closure-call “ensure arg count” nil-fill loops) has been eliminated.
- Remaining “uncovereds” are now heavily dominated by structurally defensive paths and residual
  coverage bookkeeping around infallible helper calls (addressed by making `inline_*` math/compare
  helpers and `inline_tae_string` infallible in codegen).
- The recent reduction from **187 → 168** came from removing additional structurally unreachable error
  fallthroughs (default-binding loops, infallible list destructure helpers), adding a small
  coverage-only driver for remaining free-variable scan option arms, adding a focused
  driver to exercise f-string/pipe error propagation, and removing a couple of import bookkeeping
  micro-misses (plus a coverage-only unit test for the lambda saved-block-none restore path).
- The most recent reduction from **163 → 159** came from adding coverage-only drivers for:
  - `compile_ternary` error-propagation (`gin ... than ... ither ...`)
  - `compile_list_index_fast` object compilation error propagation (list literal contains an undefined variable)
- The most recent reduction from **159 → 156** came from adding coverage-only drivers for:
  - `compile_for_iterable` iterable compilation error propagation (`fer x in __undef { ... }`)
  - `compile_for_iterable` body compilation error propagation (string loop body compile error)
- The most recent reduction from **156 → 154** came from adding coverage-only drivers for:
  - `compile_while` condition compilation error propagation (undefined condition variable, forces fallback path)
  - `compile_while` body compilation error propagation (undefined variable in loop body)
- The most recent reduction from **154 → 150** came from making several inline helpers infallible (they
  previously returned `Result` but could not actually return `Err`), removing structurally unreachable `?`
  error-propagation regions:
  - `inline_find_predicate`
  - `inline_index_of`
  - `inline_starts_wi`
- The most recent reduction from **150 → 148** came from removing two structurally unreachable `break`
  branches in the default-binding param-name loop (the loop bounds already guarantee `get(..)` success).
- The remaining missed regions are now much more diffuse; the only repeated misses tend to be in
  structurally defensive import-resolution fallthroughs (e.g., `current_exe()` / parent probing).

Some of these look like “safety net / invariant” branches; for those, prefer coverage-only driver helpers over altering language semantics just to make them reachable.

## Guardrails invariants

`scripts/coverage_guardrails.sh` enforces:

- floors: `functions.percent == 100` and `instantiations.percent == 100`
- hotspot ordering: uncovered regions in `src/interpreter.rs` must remain `<=` uncovered regions in `src/llvm/codegen.rs`

Current ordering is satisfied: `interpreter 148 <= codegen 148`.

## Recommendations

1. **Keep coverage floors stable:** always re-run `scripts/coverage_guardrails.sh` after coverage-focused changes.
2. **Cool the remaining `codegen.rs` hotspot by targeting high-density missed segments** (especially closure-as-value and closure call edge-cases), but avoid semantics changes when a branch is intentionally defensive.
3. **Prefer golden/integration programs** when they exercise user-facing behavior; use `cfg(all(feature="llvm", coverage))` unit tests for internal-only invariants that are hard to reach naturally.
