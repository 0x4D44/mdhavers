# Code Coverage Update (LLVM-Cov)

Date: 2025.12.30  
Repo: `mdhavers`  

## Canonical command (authoritative scoreboard)

Always run with a timeout to avoid hangs:

```bash
timeout 1800 cargo llvm-cov --no-default-features --features cli,llvm,native \
  --summary-only --json --output-path target/llvm-cov-summary.json
```

## Current totals (from `target/llvm-cov-summary.json`)

- Lines: **62939 / 63044 (99.83%)**
- Regions: **111329 / 111701 (99.67%)**
- Functions: **3437 / 3437 (100.00%)**
- Instantiations: **6044 / 6044 (100.00%)**

Coverage target (**≥ 98%**) remains satisfied with buffer.

## Key constraints (still satisfied)

- Floors:
  - `functions.percent == 100`
  - `instantiations.percent == 100`
- Hotspot ordering:
  - `src/interpreter.rs` missed regions **177**
  - `src/llvm/codegen.rs` missed regions **180**
  - Interpreter remains **below** codegen.

## Changes made in this iteration

### Dependency-instance coverage (tests/)

- Added coverage-only integration coverage for `get_error_suggestion` branches in the **dependency crate instance**:
  - `tests/error_suggestion_instantiation_coverage_only.rs`
    - Unexpected token closing brace (`found == "}"`)
    - Unexpected token equals (`found == "="`, expected contains `"expression"`)

- Added coverage-only integration coverage for compiler “tri import requires alias” error in the **dependency crate instance**:
  - `tests/compiler_tri_alias_error_instantiation_coverage_only.rs`

### Binary-unit coverage (src/main.rs)

- Added deterministic unit-test coverage for additional CLI helper branches (parse/runtime/verbose/warning/prelude-error) in `src/main.rs`.
- Implemented a **test-only** thread-local toggle to force `load_prelude()` to return an error inside `run_file`/`trace_file`, so the “Error loading prelude” paths are coverable without mutating process CWD or the on-disk stdlib.

## Remaining small misses (top non-hotspots)

From `target/llvm-cov-summary.json` (src-only, canonical run):

- `src/main.rs`: **2** missed lines, **8** missed regions
- `src/error.rs`: **1** missed line, **1** missed region
- `src/compiler.rs`: **2** missed lines, **2** missed regions

The dominant region gaps remain:

- `src/llvm/codegen.rs`: **180** missed regions
- `src/interpreter.rs`: **177** missed regions

