# 2025.12.23 - CR - Remediation Plan (mdhavers)

## Metadata
- Repo: `mdhavers` (`/home/md/language/mdhavers`)
- Date: 2025-12-23
- Git HEAD (uncommitted working tree): `746e15dd240cb90576e82ad103032c837f7f3594`
- Source report: `wrk_docs/2025.12.23 - CR - Code Review Report.md`
- Tracking summary: `wrk_docs/2025.12.23 - CR - Remediation Summary.md`

## Goals
1) Reduce user-triggerable crashes/panics and state corruption in the interpreter.
2) Improve build portability (especially wasm32 and opt-in LLVM).
3) Reduce backend/tooling drift via parity tests and shared front-end logic.
4) Improve maintainability and developer ergonomics (tests, feature gating, tooling).

## Status legend
- ✅ done
- 🟡 in progress
- ⏳ planned
- 💤 deferred (needs a design decision / out-of-scope refactor)

## Remediations (prioritized)

### P0 — Correctness / crash safety
- ✅ Interpreter state restoration via RAII guards (`src/interpreter.rs`)
- ✅ Checked integer arithmetic + overflow errors; fixed int-vs-float comparisons (`src/interpreter.rs`)
- ✅ Module cache + circular import detection (`src/interpreter.rs`)

### P0 — Build portability / wasm
- ✅ Host/network/crypto moved behind `native` feature and wasm32 stubs added (`Cargo.toml`, `src/interpreter.rs`)
- ✅ Playground wasm build works: `cd playground && cargo build --target wasm32-unknown-unknown`

### P1 — Build system portability (LLVM backend)
- ✅ `build.rs` gated on `llvm`, uses `OUT_DIR` artifacts, rejects wasm32 targets (`build.rs`, `src/llvm/compiler.rs`)
- ✅ Nested `mdh_runtime_rs` cargo build now uses an `OUT_DIR`-scoped `--target-dir` (avoids writing build artifacts into `runtime/mdh_runtime_rs/target`) (`build.rs`)
- 💤 Remove nested cargo invocation from `build.rs` entirely (requires redesign of how `runtime/mdh_runtime_rs` is produced/embedded; left as a follow-up project)

### P1 — Tooling / editor correctness
- ✅ LSP diagnostics now use real lexer+parser (`src/lsp/mdhavers_bindings.rs`)
- ✅ VSCode extension selects a real existing LSP binary path (`editor/vscode/src/extension.ts`)
- ✅ CLI binary uses the `mdhavers` library crate (avoid double-compiling modules) (`src/main.rs`, `src/lib.rs`)
- ✅ `wasm_runner` is exported from the library and builds with current `wasmtime` (`src/wasm_runner.rs`, `src/lib.rs`)
- ✅ Native object/property/method errors inherit correct source line context (notably tri) (`src/error.rs`, `src/interpreter.rs`)
- ✅ REPL supports multiline blocks/strings with a continuation prompt and `:cancel` (`src/main.rs`)

### P1 — Backend parity + runtime size
- ✅ JS compiler: emit `tri` runtime only when `tri` is imported (`src/compiler.rs`)
- ✅ Added interpreter-vs-JS parity smoke test (`tests/backend_parity.rs`)
- ✅ WASM compiler: reduce host import surface to only what the program uses (`src/wasm_compiler.rs`)

### P2 — Test/devex improvements
- ✅ Feature-aware CLI tests (`tests/cli_coverage.rs`)
- ✅ Replace hardcoded `target/debug/*` paths in tests with Cargo-provided binary paths (e.g. `CARGO_BIN_EXE_*`) (`tests/cli_coverage.rs`, `tests/lsp_coverage.rs`)
- ✅ Make LLVM-enabled clippy runs warning-clean (fix warnings in `runtime/mdh_runtime_rs`, and/or adjust build strategy)
- ✅ Gate the very large `tests/llvm_comprehensive_tests.rs` suite behind an opt-in feature (`llvm_comprehensive_tests`) to keep default developer runs fast
- ✅ Make minimal `--no-default-features --features cli` builds warning-clean under `-D warnings`
- ✅ Installer docs no longer advertise remote release install (`installer/install.sh`, `installer/README.md`)
- ✅ Document stdlib “contract” (stability/portability/sandboxing notes) (`stdlib/README.md`)
- ✅ Clarify audio asset acquisition/rebuild steps (SoundFont + WASM helper) (`assets/soundfonts/README.md`, `README.md`, `docs/audio.md`)
- ✅ Ignore common generated artifacts (release packages, wasm-pack output, large binary assets, example build outputs, vendor byproducts) (`.gitignore`)
- ✅ Document vendored dependency provenance and update workflow (`vendor/README.md`)
- ✅ Reconcile vendored `raylib-sys` tree (stage previously-untracked upstream files) (`vendor/raylib-sys-5.5.1/`)
- ✅ Document VSCode extension build + LSP wiring (`editor/vscode/README.md`)
- ✅ Fix README to match actual CLI subcommands (`wasm` instead of `compile --target wat`) (`README.md`)
- ✅ Add missing stdlib + examples sources referenced by docs (`stdlib/*.braw`, `examples/**/*.braw`)
- ✅ Remove duplicate/extensionless doc artifact (`wrk_docs/2025.12.19`)

### P2 — Performance / maintainability
- ✅ Lexer performance + accurate position tracking for multiline tokens (`src/lexer.rs`)
- 💤 WAT backend import ABI redesign (bigger change; may follow after usage-based import pruning)
- 💤 Repo hygiene for vendored surfaces (`editor/vscode/node_modules/`, `vendor/`) requires policy decision

## Acceptance criteria (for planned items)
### Lexer perf/position
- No O(n²) rescanning for column tracking on long lines.
- Correct `line`/`column` for multiline string literals and comments (as per language intent).
- `cargo test --lib --no-default-features --features cli` passes.

### WASM import surface minimization
- Generated WAT imports only what is needed by the compiled program (at minimum: avoid importing audio/network/tri helpers if unused).
- Existing wasm compiler tests updated appropriately (or extended to assert “imports included when used”).

### Binary-path test robustness
- `tests/cli_coverage.rs` and `tests/lsp_coverage.rs` locate binaries using `CARGO_BIN_EXE_mdhavers` / `CARGO_BIN_EXE_mdhavers-lsp` (fallbacks only if needed).

### LLVM clippy hygiene
- `cargo clippy --no-default-features --features cli,llvm,native -- -D warnings` passes (or documented exception if intentionally not supported).

### LLVM comprehensive test suite (opt-in)
- Default LLVM test runs do not execute `tests/llvm_comprehensive_tests.rs` unless explicitly enabled.
- Full sweep command:
  - `cargo test --test llvm_comprehensive_tests --no-default-features --features cli,llvm,native,llvm_comprehensive_tests`
