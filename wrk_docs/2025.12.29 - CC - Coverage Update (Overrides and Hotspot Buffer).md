# Coverage Update (Overrides and Hotspot Buffer)

Date: 2025-12-29

## Canonical Coverage Command (unchanged)

```
timeout 1800 cargo llvm-cov --no-default-features --features cli,llvm,native --summary-only --json --output-path target/llvm-cov-summary.json
```

## Current Canonical Totals

From `target/llvm-cov-summary.json`:

- Lines: **99.833%** (62,854 / 62,959)
- Regions: **99.668%** (111,137 / 111,507) — **370** uncovered
- Functions: **100%** (3,423 / 3,423)
- Instantiations: **100%** (6,030 / 6,030)

## Hotspot Status (Canonical)

Missed regions (lower is better):

- `src/llvm/codegen.rs`: **180** (6 lines)
- `src/interpreter.rs`: **178** (95 lines)

Hotspot objective remains true: **`src/interpreter.rs` missed regions < `src/llvm/codegen.rs`**.

## What Changed (This Iteration)

### Coverage improvements (small-miss files)

- `src/error.rs`: Added a missing keyword alias (`"len"`) to hit the remaining suggestion arm.
- `src/compiler.rs`: Added a compile test for `keek x { }` to cover the empty-arms conditional.
- `src/lib.rs`: Added tests to cover error-propagation branches in `run_with_output` and `compile_to_llvm_ir`.

### Interpreter hotspot buffer work

- `src/interpreter.rs`: Extended the socket helper coverage test to hit additional dict/branch paths without introducing new uncovered regions.

### Reduced process-global mutations (coverage/test overrides)

To avoid flaky coverage tests that mutate process-wide state:

- Added thread-local overrides in `src/interpreter.rs` (available under `cfg(test)` / `cfg(coverage)`):
  - MDH shell override used by `shell` / `shell_status` without `std::env::set_var("MDH_SHELL", ...)`
  - Forced `current_dir()` failure used by `cwd()` without `std::env::set_current_dir(...)`
- Updated coverage-only tests to use these overrides instead of touching global env/CWD.
- Added a unit-test instance exercise to keep **instantiations at 100%** after introducing the override helpers.

## Remaining Top Misses (Canonical Snapshot)

Regions not covered, then lines not covered (from `target/llvm-cov-summary.json`):

- `src/llvm/codegen.rs`: 180, 6
- `src/interpreter.rs`: 178, 95
- `src/main.rs`: 5, 1
- `src/compiler.rs`: 2, 2
- `src/error.rs`: 1, 1
- `src/wasm_compiler.rs`: 2, 0
- `src/parser.rs`: 1, 0
- `src/value.rs`: 1, 0

## Suggested Next Steps

1. Reduce `src/interpreter.rs` missed regions further to create more buffer before touching LLVM codegen.
2. After the interpreter buffer is comfortable, begin targeting `src/llvm/codegen.rs` (keep `functions==100` and `instantiations==100`).
3. Clean up remaining “small misses” (`src/main.rs`, `src/compiler.rs`, `src/error.rs`) with minimal deterministic tests.

