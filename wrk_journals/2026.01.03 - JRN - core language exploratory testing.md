# Journal — core language exploratory testing (2026-01-03)

## Summary
- Closed the remaining `ceilidh` parity gap: native LLVM backend now matches interpreter behavior (list interleave only; errors on non-lists).
- Re-ran golden tests and coverage guardrails with timeouts; everything is green.

## Work log

### `ceilidh` parity fix
- Interpreter behavior: `ceilidh(list1, list2)` interleaves elements; it rejects non-lists (`"ceilidh needs two lists"`).
- LLVM backend previously had a special-case that merged dicts in `ceilidh(dict1, dict2)`, which broke parity and the new golden regression.
- Updated LLVM codegen so `ceilidh`:
  - Type-checks both args are lists.
  - Calls the existing `inline_ceilidh` interleave implementation for list inputs.
  - `hurl`s with `"ceilidh needs two lists"` for any non-list inputs (catchable via `hae_a_bash`).
- Removed the now-unused `inline_dict_merge` helper from `src/llvm/codegen.rs` (dict merging remains available via `dict_merge`).

### `birl` parity + aliasing fix
- Added a golden regression to lock `birl` behavior across backends:
  - `tests/golden/edge_cases/birl_rotation_and_type_errors.braw`
  - `tests/golden/edge_cases/birl_rotation_and_type_errors.expected`
- Issues found (LLVM backend only):
  - No type-checking for `birl` args: wrong types could silently misbehave (or worse, treat non-list data as a list pointer).
  - Returned the original list for `birl([], n)` and `birl(list, 0)`, causing aliasing differences vs the interpreter (which always returns a fresh list).
- Fixes applied (LLVM codegen):
  - `birl` now type-checks `(list, int)` and `hurl`s with interpreter-style messages:
    - `"birl needs a list"`
    - `"birl needs a rotation count"`
  - `inline_birl` now returns a fresh list even when the rotation is a no-op (uses `list_slice` to copy), matching interpreter semantics.

### `sumaw` parity fix
- Added a golden regression to lock `sumaw` behavior across backends:
  - `tests/golden/edge_cases/sumaw_type_errors_and_float_parity.braw`
  - `tests/golden/edge_cases/sumaw_type_errors_and_float_parity.expected`
- Issues found (LLVM backend only):
  - `sumaw` did not type-check the input was a list (could miscompile or crash on non-lists).
  - Accepted booleans as “intlike” values (interpreter rejects them).
  - Returned `naething` on invalid elements instead of throwing a catchable error (interpreter throws).
- Fixes applied (LLVM codegen):
  - `sumaw` now requires a list and `hurl`s `"sumaw() expects a list"` on non-lists.
  - List elements must be `int` or `float` only; `aye/nae` now trigger a catchable `"sumaw() expects a list of numbers"` error.

### `radians` / `degrees` parity fix
- Added a golden regression to lock numeric conversion and error behavior:
  - `tests/golden/edge_cases/radians_degrees_type_errors_parity.braw`
  - `tests/golden/edge_cases/radians_degrees_type_errors_parity.expected`
- Issues found (LLVM backend only):
  - Accepted `aye/nae` as numeric inputs (treated as “intlike”), unlike the interpreter.
  - Returned `naething` on type errors instead of throwing a catchable error.
- Fixes applied (LLVM codegen):
  - `radians` and `degrees` now accept only `int`/`float`; non-numbers `hurl` with interpreter-style messages:
    - `"radians() needs a number"`
    - `"degrees() needs a number"`

### `floor` / `ceil` / `round` / `sqrt` parity fix
- Added a golden regression for numeric builtins on ints/floats + type errors:
  - `tests/golden/edge_cases/floor_ceil_round_sqrt_parity.braw`
  - `tests/golden/edge_cases/floor_ceil_round_sqrt_parity.expected`
- Issues found (LLVM backend only):
  - `floor`/`ceil`/`round` treated integer inputs as float bit-patterns (incorrect results).
  - `sqrt` treated any non-float as an integer (e.g., strings/bools could be interpreted as ints).
  - Type errors were not consistently catchable or message-aligned with the interpreter.
- Fixes applied (LLVM codegen):
  - `floor`/`ceil`/`round` now accept `int` (identity) and `float` (intrinsic + int result); non-numbers `hurl`:
    - `"floor() expects a number"`, `"ceil() expects a number"`, `"round() expects a number"`
  - `sqrt` now accepts only `int`/`float` and `hurl`s `"sqrt() expects a number"` for other types.

### `abs` parity fix
- Added a golden regression for `abs` on ints/floats + type errors:
  - `tests/golden/edge_cases/abs_type_errors_and_float_parity.braw`
  - `tests/golden/edge_cases/abs_type_errors_and_float_parity.expected`
- Issue found (LLVM backend only): treated any non-float as an integer, so strings/bools could be interpreted as ints (no type error).
- Fix applied (LLVM codegen): `abs` now accepts only `int`/`float` and `hurl`s `"abs() expects a number"` otherwise.

### Validation (all run with timeouts)
- `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
- `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
- `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
  - Functions: `3461/3461` (100%)
  - Instantiations: `6067/6067` (100%)
  - Hotspot invariant: interpreter missed regions `164` <= codegen missed regions `357`

## Next steps
- Continue core-language exploratory programs and add golden regressions when parity gaps are found.
- Keep using `timeout` on long-running commands to avoid getting stuck.

## Coverage regression: `inline_pooer` instantiation coverage
- Regression discovered on 2026-01-03 after the `pow`/`pooer` split + new `inline_pooer` implementation:
  - `scripts/coverage_guardrails.sh` failed with instantiation coverage `6067/6069` (99.967%).
  - The only file with an instantiation shortfall was `src/llvm/codegen.rs` (`730/732`).
- Diagnosis:
  - Generated an instantiations report via:
    - `timeout 2400 cargo llvm-cov --no-default-features --features cli,llvm,native --text --show-instantiations --output-path target/llvm-cov-text-inst-current.txt`
  - Found two `Unexecuted instantiation` entries:
    - `<mdhavers::llvm::codegen::CodeGen>::inline_pooer`
    - `<mdhavers::llvm::codegen::CodeGen>::inline_pooer::{closure#0}`
- Fix:
  - Added a coverage-only unit test in `src/llvm/codegen.rs`:
    - `inline_pooer_is_exercised_for_instantiation_coverage`
    - Compiles a tiny program containing `pooer(2, 3)` via `LLVMCompiler::compile_to_ir`, ensuring the unit-test crate instance executes `inline_pooer` and its `unwrap_or_else` closure.
  - Also expanded the coverage-only LLVM codegen sweep and CLI coverage fixture to include math/pow/pooer calls (useful, but the unit-test instance was the missing piece).
  - Removed `unused_mut` locals in `inline_pooer` (cleanup).
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `163` <= codegen missed regions `359`

## Bitwise builtins parity + UB hardening (LLVM backend)
- Added a golden regression covering bitwise operations + catchable type/range errors:
  - `tests/golden/edge_cases/bitwise_ops_shift_range_and_type_errors_parity.braw`
  - `tests/golden/edge_cases/bitwise_ops_shift_range_and_type_errors_parity.expected`
- Issues found (LLVM backend only):
  - `bit_an`/`bit_or`/`bit_xor` were wired to runtime helpers that silently returned `0` on non-integer inputs (interpreter throws).
  - `bit_nae`, `bit_coont`, `bit_shove_left`, `bit_shove_right` did not type-check arguments (could treat non-ints as ints).
  - `bit_shove_left`/`bit_shove_right` did not enforce the interpreter’s shift range rule (0–63), risking LLVM UB for shifts >= 64.
  - `bit_shove_right` used a *logical* right shift; interpreter uses arithmetic right shift for signed integers.
- Fixes applied (LLVM codegen):
  - `bit_an`/`bit_or`/`bit_xor` now require `(int, int)` and `hurl` on type errors (catchable), matching interpreter behavior.
  - `bit_nae` now requires `int` and `hurl`s on non-int.
  - `bit_shove_left`/`bit_shove_right` now:
    - require `(int, int)`,
    - `hurl` on shift amounts < 0 or > 63 (same behavior as interpreter),
    - and `bit_shove_right` now uses **arithmetic** right shift.
  - `bit_coont` now requires `int` and `hurl`s on non-int.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `163` <= codegen missed regions `359`

## Base conversion builtins parity (`tae_*` / `fae_*`)
- Added a golden regression covering base conversions + catchable errors:
  - `tests/golden/edge_cases/base_conversion_parity_and_errors.braw`
  - `tests/golden/edge_cases/base_conversion_parity_and_errors.expected`
- Issues found (LLVM/native runtime only):
  - `tae_binary`/`tae_hex`/`tae_octal` returned `"0"` on non-int instead of throwing a catchable error.
  - Negative formatting did **not** match Rust (`format!("{:b|:x|:o}", i64)`): the runtime used a `-...` sign style rather than two’s-complement formatting.
  - `fae_binary`/`fae_hex` were permissive: they silently skipped invalid characters and returned `0` on type errors instead of throwing; this diverged from the interpreter’s strict `from_str_radix` behavior.
- Fixes applied (C runtime in `runtime/mdh_runtime.c`):
  - `tae_binary`/`tae_hex`/`tae_octal` now `hurl` on non-int inputs (catchable).
  - Formatting now matches Rust signed formatting semantics by treating the value as `uint64_t` and printing without a `-` sign.
  - `fae_binary`/`fae_hex` now `hurl` on non-string inputs (catchable), validate all digits (after optional `0b`/`0x` prefix), and `hurl` on invalid/empty/overflow parses.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `162` <= codegen missed regions `359`

## `center()` fill semantics: empty fill string defaults to space
- Added a small golden regression to lock interpreter/native parity around fill handling:
  - `tests/golden/edge_cases/center_fill_empty_string_parity.braw`
  - `tests/golden/edge_cases/center_fill_empty_string_parity.expected`
- Note: the runtime helper `__mdh_utf8_first_codepoint_bytes` already treats empty strings as a space fill, so no runtime change was required.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅

## `coont(list, needle)` parity: use value equality (not pointer equality) in native runtime
- Added a golden regression that creates equal-but-distinct values (strings + lists) and counts occurrences:
  - `tests/golden/edge_cases/coont_list_value_equality_parity.braw`
  - `tests/golden/edge_cases/coont_list_value_equality_parity.expected`
- Issue found (LLVM/native runtime only):
  - `coont(list, needle)` in the LLVM backend used the runtime `__mdh_count_val`, which compared `tag` + `data` (pointer-ish) instead of language equality.
  - This broke counting for equal-but-distinct strings/lists (e.g. `"h" + "i"` vs `"hi"`, `[1]` literals).
- Fix applied (C runtime in `runtime/mdh_runtime.c`):
  - `__mdh_count_val` now uses `__mdh_eq(item, val)` to match interpreter `Value::eq` semantics.
  - `__mdh_list_index` also now uses `__mdh_eq` for consistency with “find index of value” behavior.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `162` <= codegen missed regions `359`

## `unique(list)` parity: use value equality (not pointer equality) in native runtime
- Added a golden regression that constructs equal-but-distinct strings via concatenation and ensures `unique()` dedupes:
  - `tests/golden/edge_cases/unique_list_value_equality_parity.braw`
  - `tests/golden/edge_cases/unique_list_value_equality_parity.expected`
- Issue found (LLVM/native runtime only):
  - `unique(list)` used the C runtime helper `__mdh_unique`, which compared `tag` + `data` and therefore failed to dedupe equal-but-distinct values (e.g. `"h" + "i"` vs `"hi"`).
- Fix applied (C runtime in `runtime/mdh_runtime.c`):
  - `__mdh_unique` now uses `__mdh_eq(dst->items[j], item)` to match interpreter `Value::eq` semantics (deep for strings/lists/bytes; pointer-ish for dict/class/instance/etc).
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `163` <= codegen missed regions `359`

## `average()` / `median()` parity: fix placeholder semantics in native runtime
- Added a golden regression that checks:
  - `average()` correct results
  - `median()` correct results (including cases where median != mean)
  - both functions throw (catchably) on empty lists and non-number elements
  - `tests/golden/edge_cases/average_median_parity_and_errors.braw`
  - `tests/golden/edge_cases/average_median_parity_and_errors.expected`
- Issues found (LLVM/native runtime only):
  - `__mdh_average` returned `0.0` for non-lists/empty lists and silently ignored non-number elements.
  - `__mdh_median` was a placeholder that delegated to `__mdh_average` (not a true median).
- Fixes applied (C runtime in `runtime/mdh_runtime.c`):
  - `__mdh_average` now:
    - `hurl`s on non-list inputs,
    - `hurl`s on empty lists,
    - `hurl`s on any non-number element,
    - returns a float `sum / len` (matching interpreter).
  - `__mdh_median` now:
    - `hurl`s on non-list inputs and empty lists,
    - validates all elements are numeric,
    - rejects NaN floats (`median() cannae handle NaN`),
    - sorts numeric values and returns the correct median (float).
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `155` <= codegen missed regions `359`

## `product()` parity: numeric semantics + error handling in LLVM backend
- Added golden regressions:
  - `tests/golden/edge_cases/product_numeric_type_parity.braw`
  - `tests/golden/edge_cases/product_numeric_type_parity.expected`
  - `tests/golden/edge_cases/product_error_handling_parity.braw`
  - `tests/golden/edge_cases/product_error_handling_parity.expected`
- Issues found (LLVM backend only):
  - `product()` was implemented as raw `i64` multiplication over element `.data` with no type checks:
    - float elements produced incorrect results and returned an `integer` tag (parity break),
    - non-list inputs could read arbitrary memory (potential crash/UB).
- Fix applied (LLVM codegen in `src/llvm/codegen.rs`):
  - `inline_product` now:
    - type-checks list input and `hurl`s on non-list (`product() needs a list`),
    - multiplies using an `f64` accumulator to match interpreter semantics (no integer overflow errors),
    - returns `float` if any float element was seen, else returns `integer`,
    - `hurl`s on any non-number element (`product() needs a list o' numbers`),
    - uses a saturating float→int conversion when returning an integer to avoid LLVM UB and match Rust `as i64` behaviour.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `153` <= codegen missed regions `359`

## `shuffle()` parity: throw on non-list inputs in native runtime
- Added a golden regression:
  - `tests/golden/edge_cases/shuffle_type_error_parity.braw`
  - `tests/golden/edge_cases/shuffle_type_error_parity.expected`
- Issue found (LLVM/native runtime only):
  - `shuffle("hello")` returned an empty list (so `len(...) == 0`) instead of throwing a catchable error like the interpreter.
- Fix applied (C runtime in `runtime/mdh_runtime.c`):
  - `__mdh_shuffle` now `hurl`s on non-list inputs with `shuffle() expects a list`.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `153` <= codegen missed regions `359`

## `sort()` / `unique()` / `uniq()` parity: mixed-type stability + empty-list copy semantics
- Added golden regressions:
  - `tests/golden/edge_cases/list_helpers_empty_list_returns_copy_parity.braw`
  - `tests/golden/edge_cases/list_helpers_empty_list_returns_copy_parity.expected`
  - `tests/golden/edge_cases/sort_mixed_types_parity_and_errors.braw`
  - `tests/golden/edge_cases/sort_mixed_types_parity_and_errors.expected`
- Issues found (LLVM/native runtime only):
  - `sort([])`, `unique([])`, and `uniq([])` returned the *same* empty list object (aliasing), so mutating the result mutated the original list (interpreter returns a new list).
  - `sort("hello")` returned the input (no error) instead of throwing a catchable error like the interpreter.
  - Mixed-type `sort(...)` could abort due to the runtime using `__mdh_lt/__mdh_gt` in a qsort comparator (type errors on mismatched tags), and it also differed from the interpreter’s “stable barrier” behaviour for mixed types.
- Fixes applied (C runtime in `runtime/mdh_runtime.c`):
  - `__mdh_unique` now:
    - `hurl`s on non-list inputs (`unique() needs a list`),
    - returns a fresh empty list for empty input (no aliasing).
  - `__mdh_list_uniq` now:
    - `hurl`s on non-list inputs (`uniq() needs a list`),
    - returns a fresh empty list for empty input (no aliasing).
  - `__mdh_list_sort` now:
    - `hurl`s on non-list inputs (`sort() expects a list`),
    - returns a fresh empty list for empty input (no aliasing),
    - sorts **within contiguous runs of the same tag** using a stable comparator (mirrors the interpreter’s stable sort where mismatched-type comparisons are `Equal`, so types act as stable barriers).
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `152` <= codegen missed regions `359`

## `minaw()` / `maxaw()` parity: singleton non-number lists should error
- Added golden regression:
  - `tests/golden/edge_cases/minaw_maxaw_singleton_type_check_parity.braw`
  - `tests/golden/edge_cases/minaw_maxaw_singleton_type_check_parity.expected`
- Issue found (interpreter only):
  - `minaw(["x"])` and `maxaw(["x"])` returned `"x"` because the first element’s type wasn’t validated when the list has length 1.
  - Native/LLVM already rejects non-number inputs via the runtime `list_min/list_max` helpers.
- Fix applied (interpreter in `src/interpreter.rs`):
  - `minaw`/`maxaw` now validate the first element is `int` or `float`, otherwise error with the same message used for mixed-type lists (`... needs a list o' comparable numbers`).
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `152` <= codegen missed regions `359`

## `chynge()` / `replace()` parity: list insert vs string replace miswiring
- Added golden regression:
  - `tests/golden/edge_cases/chynge_and_replace_parity.braw`
  - `tests/golden/edge_cases/chynge_and_replace_parity.expected`
- Issue found (LLVM/native codegen wiring):
  - LLVM codegen routed both `chynge(...)` and `replace(...)` calls to the runtime `__mdh_chynge`, which implemented **string** replace and silently returned the first argument unchanged on type mismatch.
  - Interpreter semantics are:
    - `chynge(list, index, value)` inserts into a list and returns a new list.
    - `replace(str, old, new)` replaces all occurrences in a string and errors on type mismatches.
- Fixes applied:
  - Runtime (`runtime/mdh_runtime.c`, `runtime/mdh_runtime.h`):
    - Repurposed `__mdh_chynge` to implement list insert (`chynge(list, index, value)`), with catchable errors on type/index issues.
    - Added `__mdh_replace` implementing string replace parity, including Rust-style empty-pattern behaviour.
    - Updated internal users (`__mdh_scottify`, `__mdh_blether_format`) to call `__mdh_replace`.
  - LLVM codegen (`src/llvm/codegen.rs`):
    - Added `__mdh_replace` to the libc function table and routed the `replace` builtin to it.
    - Routed `chynge` to `__mdh_chynge` (list insert).
  - Tests (`tests/llvm_comprehensive_tests.rs`):
    - Replaced string-`chynge(...)` calls with `replace(...)`.
    - Repurposed `test_chynge*` to exercise list insert semantics.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Lines: `66807/66944` (99.795%)
    - Regions: `119837/120348` (99.575%)
    - Hotspot invariant: interpreter missed regions `148` <= codegen missed regions `359`

## `replace_first()` parity: type errors + empty-pattern behaviour
- Added golden regression:
  - `tests/golden/edge_cases/replace_first_parity_empty_and_type_errors.braw`
  - `tests/golden/edge_cases/replace_first_parity_empty_and_type_errors.expected`
- Issues found (LLVM/native runtime only):
  - `__mdh_replace_first` silently returned the input on non-string args instead of raising a catchable error like the interpreter.
  - Empty-pattern replace (`replace_first("ab", "", "x")`) was treated as a no-op instead of matching Rust’s `replacen(..., 1)` semantics (prefix insertion once).
- Fix applied (C runtime in `runtime/mdh_runtime.c`):
  - `__mdh_replace_first` now `hurl`s on non-string inputs with `replace_first() needs three strings`.
  - `__mdh_replace_first` now matches empty-pattern behaviour by returning `new + s` when `old == ""`.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `148` <= codegen missed regions `359`

## `is_digit()` / `wheesht_aw()` parity: type errors + semantics
- Added golden regression:
  - `tests/golden/edge_cases/is_digit_wheesht_aw_parity.braw`
  - `tests/golden/edge_cases/is_digit_wheesht_aw_parity.expected`
- Issues found (LLVM/native runtime only):
  - `is_digit("123")` returned `nae` because the runtime implementation only accepted a single-character string.
  - `is_digit([1])` returned `nae` instead of raising a catchable error like the interpreter.
  - `wheesht_aw([1])` silently returned the input instead of raising a catchable error like the interpreter.
- Fix applied (C runtime in `runtime/mdh_runtime.c`):
  - `__mdh_is_digit` now checks *all* characters are ASCII digits, and `hurl`s on non-string inputs with `is_digit() needs a string`.
  - `__mdh_wheesht_aw` now `hurl`s on non-string inputs with `wheesht_aw() needs a string`.
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `148` <= codegen missed regions `359`

## `is_digit()` native parity: route builtin to runtime helper
- Issue found (LLVM codegen only):
  - `is_digit([1])` errored via the generic `type_error` path, so the native error message differed from the interpreter (`is_digit() needs a string`).
- Fix applied (LLVM codegen in `src/llvm/codegen.rs`):
  - `is_digit` / `is_numeric` now call the runtime `__mdh_is_digit` helper directly (which raises `is_digit() needs a string` on type mismatch).
- Validation (all run with timeouts):
  - `timeout 600 cargo test --test golden_tests golden_tests_interpreter` ✅
  - `timeout 900 cargo test --test golden_tests golden_tests_native --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3463/3463` (100%)
    - Instantiations: `6070/6070` (100%)
    - Hotspot invariant: interpreter missed regions `148` <= codegen missed regions `382`

## LLVM compiler: circular import detection parity + coverage guardrails regression fix
- Issue found:
  - Interpreter detects circular imports and raises `HaversError::CircularImport`, but LLVM compilation could silently tolerate cycles due to early module caching during import compilation.
- Fixes applied:
  - LLVM codegen (`src/llvm/codegen.rs`):
    - Added `import_in_progress: Vec<PathBuf>` tracking to detect cycles during `compile_import`.
    - Raised `HaversError::CircularImport { path: "<chain>" }` when a cycle is detected (chain uses ` -> ` separators).
    - Refactored `compile_import` to avoid introducing uncovered instantiation-only closures in coverage builds (explicit loop for chain construction; structured error handling so cleanup runs reliably).
    - Ensured `source_path` is restored on nested import failure paths (previously `?` could skip restoration).
  - Tests:
    - Added `tests/llvm_circular_import.rs` (feature-gated with `llvm`) to assert LLVM compilation errors on circular imports and includes both module filenames in the chain.
- Coverage regression:
  - `scripts/coverage_guardrails.sh` temporarily failed on `.totals.instantiations.percent == 100` due to a single uncovered instantiation in `src/llvm/codegen.rs` (identified via `target/llvm-cov-full.json`).
  - Resolved by removing the iterator closure in the circular-import chain builder (which was uncovered in one crate instance during coverage runs).
- Validation (all run with timeouts):
  - `timeout 900 cargo test --test llvm_circular_import --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3464/3464` (100%)
    - Instantiations: `6072/6072` (100%)
    - Hotspot invariant: interpreter missed regions `148` <= codegen missed regions `385`

## LLVM imports: alias cache-hit rebinding (fix crash on `fetch "mymod" tae m` twice)
- Issue found (LLVM backend only):
  - Importing the same module twice with an alias could crash the compiler (panic) because `compile_import` returned early on cache hit without re-binding exported names. This caused `build_module_dict()` to unwrap an `Undefined variable` error when compiling the second aliased import.
- Fixes applied (LLVM codegen):
  - `src/llvm/codegen.rs`:
    - Added `import_prefixes: HashMap<PathBuf, String>` to keep the stable LLVM name prefix for each isolated import.
    - On cached imports, `compile_import` now calls `rebind_cached_import_bindings(...)` to reinsert exported functions/globals into the current codegen scope before returning.
    - Added a coverage-only unit test to ensure `rebind_cached_import_bindings` is executed in the unit-test crate instance (to keep instantiation coverage at 100%).
- Tests:
  - Added `tests/llvm_import_alias_cache.rs` to compile-and-run a program that imports the same module twice with aliases and ensures both aliases remain callable and a subsequent unaliased import reintroduces exports.
- Coverage regression + fix:
  - `scripts/coverage_guardrails.sh` briefly regressed on instantiations (one uncovered instantiation: `CodeGen::rebind_cached_import_bindings`).
  - Fixed by adding the coverage-only unit test in `src/llvm/codegen.rs` to exercise the cache-hit path during unit tests.
- Validation (all run with timeouts):
  - `timeout 900 cargo test --test llvm_import_alias_cache --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3466/3466` (100%)
    - Instantiations: `6075/6075` (100%)
    - Hotspot invariant: interpreter missed regions `148` <= codegen missed regions `387`

## LLVM imports: `thing` (struct) declarations inside imported modules
- Issue found (LLVM backend only):
  - `fetch "mymod"` where `mymod.braw` defines `thing Point { x, y }` failed with `CompileError("Undefined variable: Point")` because module import compilation ignored `Stmt::Struct`.
- Fixes applied (LLVM codegen):
  - `src/llvm/codegen.rs`:
    - `collect_module_exports` now includes `Stmt::Struct` so aliased imports can expose (and later hide) struct constructors consistently.
    - `compile_import` now declares struct constructors in the first pass and compiles them in the second pass (`compile_struct_decl`), so imported structs are available to the importer.
- Tests:
  - Added `tests/llvm_import_struct.rs` to compile-and-run a module that exports a struct constructor and uses it after `fetch`.
- Validation (all run with timeouts):
  - `timeout 900 cargo test --test llvm_import_struct --features llvm` ✅
  - `timeout 2400 bash scripts/coverage_guardrails.sh` ✅
    - Functions: `3466/3466` (100%)
    - Instantiations: `6075/6075` (100%)
    - Hotspot invariant: interpreter missed regions `148` <= codegen missed regions `389`
