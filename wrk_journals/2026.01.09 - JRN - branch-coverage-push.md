# Journal - Branch Coverage Push

Date: 2026.01.09

## Entry 1
- Goal: drive branch coverage toward 100%.
- Started by setting up this journal and reviewing latest branch-coverage report paths and hotspots.
- Next: identify remaining missed branch sites (starting with smaller files, then interpreter/codegen).

## Entry 2
- Generated branch-miss map from `target/llvm-cov-branch-full.json`.
- Non-interpreter/codegen misses still spread across: audio.rs, compiler.rs, error.rs, formatter.rs, lexer.rs, llvm/compiler.rs, logging.rs, lsp/main.rs, main.rs, parser.rs, tri.rs, value.rs, wasm_compiler.rs.
- Next: start trimming `matches!`/guard-only misses in small files (tri/value/lexer/logging/error) before tackling compiler/wasm/main.

## Entry 3
- Added dependency-instance truthy checks in `tests/value_instantiation_sweep_coverage_only.rs` (float non-zero + empty set).
- Reworked `is_unkent_token` in `src/lexer.rs` to avoid guard-only `matches!` branches and added mismatch cases.
- Expanded logging tests to hit format-text/compact branches, file-sink branches, memory drain, and span-exit mismatch.
- Added more `get_error_suggestion` matrix cases to exercise `&&`/else branches.
- Next: rerun branch coverage and re-evaluate remaining misses; focus on lexer/logging/error deltas.

## Entry 4
- Re-ran branch coverage (nightly, skipped golden native); report updated.
- Cleared branch misses from `src/tri.rs`, `src/logging.rs`, `src/value.rs`, and `src/formatter.rs`.
- Remaining non-hotspot misses: audio.rs (6), compiler.rs (3), error.rs (6), lexer.rs (2), lsp/main.rs (2), main.rs (4), parser.rs (2), wasm_compiler.rs (6), llvm/coverage_tests.rs (1).
- Hotspots unchanged: interpreter.rs (109) and llvm/codegen.rs (89).

## Entry 5
- Resuming branch coverage push; will triage remaining small-file misses before tackling interpreter/codegen hotspots.
- Next: inspect branch-miss lines for error/lexer/main/parser/wasm/compiler/audio/lsp and add targeted tests.

## Entry 6
- Added targeted tests across lsp/error/main/compiler/parser/audio to hit remaining branch gaps (underscore identifiers, hover keyword, did-change last update; error suggestion false paths; repl/string/comment/negative nesting; format_file check-only mismatch + write error; compiler branchy constructs incl. slices/ranges/match/fstring; parser block expr/lambda/trailing comma/empty bodies/hex escapes; audio alloc_handle/soond_unlade valid + sequencer end-of-seq).
- Updated llvm coverage test to map expected errors per case (avoid `||` branch).
- Next: re-run branch coverage, inspect remaining misses (lexer/main/audio/compiler/wasm/interpreter/codegen), and decide next targeted tests.

## Entry 7
- Re-ran branch coverage successfully; overall branch coverage now 94.68% (notcovered 216). Interpreter/codegen still dominate (109 + 89 notcovered).
- Non-hotspot notcovered now small: wasm_compiler 6, audio 5, logging 2, main 2, parser 2, compiler 1.
- Next: tighten remaining non-hotspot branches (likely compiler bool pattern, parser return/escape branches, audio unlade/alloc/midi end-of-seq, logging/main small branches) then decide how deep to go into interpreter/codegen.

## Entry 8
- Added more targeted tests (audio cursor failures + unlade None-slot coverage, logging memory drain, main compile file error, wasm import requirement matrix) and reran branch coverage; run hit timeout at 300s but report updated (totals unchanged at 94.68%, notcovered 216).
- Remaining non-hotspot notcovered still 18 (audio 5, wasm_compiler 6, logging 2, main 2, parser 2, compiler 1); interpreter/codegen still 109/89.
- Next: need more precise branch mapping (likely using llvm-cov show) to identify which exact edges still missing in the small files; then decide if we tackle interpreter/codegen hotspots or scope them out.

## Entry 9
- Generated branch-site maps using rustup nightly llvm-cov `show --show-branches=count` with the current profdata + object list.
- Used JSON grouping (sum true/false by line/col) to pinpoint real misses for non-hotspot files.
- Targeted fixes queued/implemented:
  - audio.rs: cover MidiFileSequencer end_of_sequence `!playing` false branch; cover mix_midi_entry not-at-end; cover alloc_handle false branch for BufferEntry/MidiEntry instantiations.
  - wasm_compiler.rs: cover second-condition false branches in collect_locals_stmt (local_vars contains name) and ensure_temp_local false for TMP_LOGIC/TMP_BUILD.
  - parser.rs: add destructure trailing-comma (exit via RightBracket) and EOF missing right bracket case.
  - compiler.rs: add tri.braw import test.
  - logging.rs: restructure file-handle setup to cover LogSink::File match false branch and File::create error branch.
  - main.rs: add repl_needs_more_input negative bracket/paren cases.
- Next: rerun branch coverage (longer timeout) to validate deltas and re-evaluate remaining misses (including interpreter/codegen).

## Entry 10
- Added a large batch of LLVM codegen coverage-only tests and helper adjustments (nested masel capture, import alias dispatch paths, int shadow loop branches, class/method cycle breaks, compile_set/get edge paths, module init/import branches, etc.).
- Added JSON parse error branch coverage fix in interpreter (invalid exponent string now triggers correct error path).
- Re-ran branch coverage with `cargo +nightly llvm-cov --branch --no-default-features --features cli,llvm,native --json --output-path target/llvm-cov-branch-full.json`; overall branch coverage ~96.50% (notcovered 141).
- Generated llvm-cov show output using rustup nightly llvm-cov for interpreter/codegen/wasm; system llvm-cov couldn't read the profdata.
- Remaining misses concentrated in `src/interpreter.rs` (~75), `src/llvm/codegen.rs` (~65), and `src/wasm_compiler.rs` (1). Noted specific hot lines for codegen (masel capture branch, destructure ignore/rest, compile_main predefine skip, import alias class checks, dynamic operator overload cycle/param-count, module globals import, lambda masel capture add) and interpreter (JSON number parsing branches, builtin constants guards, NaN/inf check, date leap-year, float threshold, event-loop due check, slice loops).
- Next: add targeted tests to hit the remaining codegen/interpreter/wasm branch edges, then rerun branch coverage.

## Entry 11
- Simplified slice loops in `src/interpreter.rs` by removing redundant `i < len`/`i >= 0` checks to reduce unreachable branch edges.
- Added interpreter unit tests covering ModuleInProgressGuard null, dict_get_u16 negative/float bounds, overridden builtins in get_user_variables, log_init memory max <= 0, json number edge cases, json_stringify NaN/inf, gallus extremes, braw_date leap/non-leap, grup_runs branches, bytes range/offset errors, and slice step loops.
- Added native event-loop coverage test in `tests/interpreter_event_loop.rs` to exercise cancelled timers, diff < 0 normalization, timeout=0 with/without watchers, and timeout < 0 branch.
- Added LLVM codegen tests for sanitize_symbol, preregister_class flag reuse, predefine-global skip, import-alias false branches (var decl + call), nested masel capture unused path, destructure ignore-after-rest, sync_all_shadows missing variable, and index fast-path fallback.
- Added WASM compiler tests for scan/collect locals false branches, non-function compile_function path, ensure_temp_local skip, if-without-else/return-none/var decl without initializer, bool literal emission, non-variable call error, and escape_wat_string non-graphic.
- Next: rerun branch coverage, then re-evaluate remaining missing lines (esp. interpreter/codegen hot spots) and add targeted tests or small refactors where branches are logically unreachable.

## Entry 12
- Parsed `target/llvm-cov-branch-*.txt` for branch misses; current misses: interpreter (127 branch sites), codegen (90), wasm_compiler (16), compiler (46), parser (24), logging (29), main (22).
- Noted wasm_compiler `compile_function` false-branch still missing (line ~607), plus return/assert optional branches and several compile_stmt lines.
- Next: inspect each file at the reported line numbers, add targeted coverage-only tests or small refactors to hit false branches, then rerun branch coverage.

## Entry 13
- Added coverage-focused tests to hit missing branch edges in smaller files:
  - `src/wasm_compiler.rs`: new `wasm_missing_branch_matrix_for_coverage` test covering return/if/var-init None paths, collect_locals duplicates, variable call branch, and temp-local skip.
  - `src/compiler.rs`: new `compiler_branch_matrix_for_coverage` exercising tri import paths, scan-if else, class init/superclass, match arms/empty arms, assert message, destructure commas, slices (step/no-step), list/range, bool literals, call args, and fstring `$` escaping.
  - `src/parser.rs`: added coverage-only parse-error cases to hit EOF loop exits in destructure/class/struct/match/block/lambda, plus valid hex escape parsing.
  - `src/logging.rs`: new `logging_branch_matrix_for_coverage` covering LogFilter rule selection, file sink open/append failure, memory sink overflow, format_text/compact branches, span mismatch, and timestamp record.
  - `src/main.rs`: extended coverage helper to cover repl negative/in-string paths, read_file with no extension, print_environment empty, and LLVM build branches; added test for functions-only environment.
- Next: fix any compile issues from new tests, re-run branch coverage, then tackle remaining interpreter/codegen misses using updated show outputs.
